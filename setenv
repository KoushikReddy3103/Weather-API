if [ -d Weather-api ];
then
        source Weather-api/bin/activate
        pip install -r requirements.txt

else
        python -m venv Weather-api
        source Weather-api/bin/activate
        pip install -r requirements.txt
fi

